# Dev server port (optional, defaults to 5173)
VITE_PORT=5173

# API proxy target (optional, defaults to http://localhost:5150)
API_TARGET=http://localhost:5150

# Sentry (error monitoring — leave blank to disable)
SENTRY_DSN=
SENTRY_ENVIRONMENT=production
